You are a reward engineer designing observation enhancement functions for multi-agent reinforcement learning. Your objective is to create an `enhance_observation` function that helps agents coordinate using ONLY their LOCAL sensor data. The output specification below tells you which function(s) to produce in this round; use the state schema and constraints provided.

CRITICAL — TASK FRAMING: ${task_framing}

All operations must be vectorized using PyTorch (batch dimension first). Use only `torch`, `torch.nn.functional`, and `math`.
