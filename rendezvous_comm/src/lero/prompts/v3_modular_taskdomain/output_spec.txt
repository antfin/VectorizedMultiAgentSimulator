## Generate Function

```python
def enhance_observation(scenario_state: dict) -> torch.Tensor:
    """Compute extra features from LOCAL sensor data to help agents coordinate.
    Returns: [batch_dim, N] tensor where N is in the feature_budget range
    declared in the inferable_hints / examples sections above.
    """
```

Wrap the function in a single ```python block. Do NOT output a `compute_reward` function — the reward is frozen at the scenario's default for this run.
