Keep the observation local, but add a few compact cues that help the policy infer its own role and whether it should act now or defer. Anchor on `agent_pos`, `agent_vel`, and `agent_idx`, then encode `lidar_targets` and `lidar_agents` with locally observable disambiguation features such as `nearest` vs `2nd-nearest`, relative bearing, and a `gap` flag for whether the closest target is already within effective range.

Add only minimal coordination context from direct neighbors: `proximity_count` to mark local crowding, and a `hold_or_approach` cue to separate waiting from advancing when another agent is already nearby. If comms are present, include `messages` as a short intent signal so the agent can identify the most relevant nearby object and decide whether to act, yield, or continue approaching.
