# Experiments page

Lists every completed run under the active experiments root. Reads
`output/metrics.json` from each run-dir and tabulates M1–M9.

> **Stub** — full content (filters, search, leaderboard view) lands at
> F10.2 review. Implementation: `src/multi_scenario/frontend/pages/experiments.py`.
