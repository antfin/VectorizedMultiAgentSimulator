# Run detail page

Drill into one run — input config, M1–M9, per-eval-iter scalars
(`output/benchmarl/*/scalars/*.csv`), before/after-training videos.

LERO runs also show the evolution doc + per-iter prompts.

> **Stub** — full content lands at F10.2 review. Implementation:
> `src/multi_scenario/frontend/pages/run_detail.py`.
